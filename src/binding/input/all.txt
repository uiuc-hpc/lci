def COMMON_ATTRS:
  runtime_t runtime

def COMMON_ARGS:
  optional runtime_t runtime
  optional error_t* error

obj runtime:
  bool use_reg_cache
  bool use_control_channel
  bool use_default_net_context
  bool use_default_net_device
  option_rdv_protocol_t rdv_protocol

op get_rank:
  COMMON_ARGS
  int* rank
  
op get_nranks;
  optional runtime_t runtime
  optional error_t* error
  int* nranks

op alloc_runtime:
  runtime_t* runtime

op free_runtime:
  runtime_t runtime

obj net_context:
  option_backend_t backend
  std::string provider_name
  int64_t max_msg_size

op alloc_net_context:
  optional runtime_t runtime
  optional error_t* error
  optional option_backend_t backend
  optional std::string provider_name
  optional int64_t max_msg_size 
  net_context_t* net_context

op free_net_context:
  optional runtime_t runtime
  optional error_t* error
  net_context_t context

obj net_device:
  int64_t max_sends
  int64_t max_recvs
  int64_t max_cqes

op alloc_net_device:
  optional runtime_t runtime
  optional error_t* error
  optional int64_t max_sends
  optional int64_t max_recvs
  optional int64_t max_cqes
  net_device_t* device

op free_net_device:
  optional runtime_t runtime
  optional error_t* error
  net_device_t device

obj mr:

op register_memory:
  optional runtime_t runtime
  optional error_t* error
  net_device_t device
  void* address
  size_t size
  mr_t* mr

op deregister_memory:
  optional runtime_t runtime
  optional error_t* error
  mr_t mr

op get_rkey:
  optional runtime_t runtime
  optional error_t* error
  rkey_t* rkey

op net_poll_cq:
  optional runtime_t runtime
  optional error_t* error
  net_device_t device
  optional int max_polls
  std::vector<net_status_t>* statuses

obj net_endpoint:

op alloc_net_endpoint:
  optional runtime_t runtime
  optional error_t* error
  net_endpoint_t* net_endpoint

op free_net_endpoint:
  optional runtime_t runtime
  optional error_t* error
  net_endpoint_t net_endpoint
